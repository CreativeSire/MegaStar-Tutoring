export default function ClientReschedulePage() {
  return (
    <section className="panel">
      <h2>Request a reschedule</h2>
      <p>Suggest an alternative time when a lesson needs to move.</p>
    </section>
  );
}
